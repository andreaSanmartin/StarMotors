package com.develop.app.repository;

import com.develop.app.model.TblTipoCombustible;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TipoCombustibleRepository extends JpaRepository<TblTipoCombustible, Integer>{
}
