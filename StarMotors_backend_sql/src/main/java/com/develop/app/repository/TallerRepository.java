package com.develop.app.repository;

import com.develop.app.model.TblTaller;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TallerRepository extends JpaRepository<TblTaller, Integer> {
}
