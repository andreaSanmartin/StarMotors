import { FiltroGarantiaPipe } from './filtro-garantia.pipe';

describe('FiltroGarantiaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroGarantiaPipe();
    expect(pipe).toBeTruthy();
  });
});
