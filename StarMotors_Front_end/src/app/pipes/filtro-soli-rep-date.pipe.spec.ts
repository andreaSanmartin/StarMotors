import { FiltroSoliRepDatePipe } from './filtro-soli-rep-date.pipe';

describe('FiltroSoliRepDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroSoliRepDatePipe();
    expect(pipe).toBeTruthy();
  });
});
