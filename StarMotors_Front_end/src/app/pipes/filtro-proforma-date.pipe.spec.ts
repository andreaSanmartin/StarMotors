import { FiltroProformaDatePipe } from './filtro-proforma-date.pipe';

describe('FiltroProformaDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroProformaDatePipe();
    expect(pipe).toBeTruthy();
  });
});
