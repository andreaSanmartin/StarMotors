import { FiltroDetallePipe } from './filtro-detalle.pipe';

describe('FiltroDetallePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroDetallePipe();
    expect(pipe).toBeTruthy();
  });
});
